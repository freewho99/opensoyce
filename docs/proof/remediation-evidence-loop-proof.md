# PR-PROOF-3 — Remediation Evidence Closes the Evidence Loop

## Status

Complete.

This proof demonstrates the PR-16C Fix Evidence Loop on live production, continuing the PR-PROOF-2 chain:

reviewer direction `remediation_required`
→ derived case `awaiting_evidence`
→ real follow-up observation of the fixed version
→ human-cited remediation evidence recorded
→ derived case `evidence_recorded`
→ evidence export with all ten sections present

## Core claim

A `remediation_required` direction does not complete remediation. It opens an evidence case. A human records cited remediation evidence. OpenSoyce preserves the link from original observation to reviewer direction to evidence, without mutating the past or certifying the fix.

OpenSoyce now separates "we decided this needs remediation" from "someone cited evidence that remediation happened."

## Summary

The PR-PROOF-2 chain ended with reviewer resolutions of `remediation_required` on exception `888aae0f` (lodash@4.17.20, expired by clock time, reaped by the system). At that moment the proof itself named the gap: a recorded direction is not a completed action.

PR-16C closed the gap as a lane; this proof closes it on the record. The record is accelerated, not fabricated: the fixed version was observed through a real production CLI ingest, the evidence was recorded through the deployed runtime, the derived case status moved by read-time derivation only, and no historical record changed.

## The run

| | |
|---|---|
| Date | 2026-06-12, ~03:25–03:26 UTC |
| Target | production (`www.opensoyce.com`) |
| Actor | `@freewho99` |
| Workspace | `opensoyce` |
| Case | exception `888aae0f-eb07-4eec-a16a-a093294b8c76` (the PR-PROOF-2 chain) |

## The closure, with receipts

**1 — The case, before.** `GET .../exceptions/888aae0f/remediation-evidence` returned derived `case_status: awaiting_evidence`, zero evidence rows. The direction existed (two `remediation_required` resolutions from PR-PROOF-2); nothing had answered it.

**2 — The fixed version was observed for real (03:25:39Z).** `opensoyce exposure ingest-dependencies` ingested `lodash@4.17.21` — the fixed version for `GHSA-35jh-r3h4-6jhm` — creating exposure `28f6c108-60ec-4841-88c9-85af11ab086f`. The citation is an observation in the record, not a note.

**3 — The human recorded the evidence (03:26:07Z).** Evidence `eccefc1a-9a86-4a62-9f24-63ce44dd4501`:

* type: `fixed_version_observed`
* reference: `lodash@4.17.21 observed through follow-up CLI ingest (exposure 28f6c108-60ec-4841-88c9-85af11ab086f)`
* recorded by: `@freewho99`
* answers reviewer direction: `c2d42dd3-9b8b-481e-b2c5-7592ec7a8253`
* chain link discovered by the runtime: source exposure `65d32e84` (via the 6D proposal event — the original observation, found, not asserted)

**4 — The case, after.** Derived `case_status: evidence_recorded`, one evidence row. The exception state: **still `expired`**. The resolutions: untouched. The question: untouched. The case moved because the record grew, not because anything was edited.

**5 — The export (03:26:33Z).** The evidence bundle for the original exposure `65d32e84` regenerated with **all ten sections present, honest-edges `missing: []`** — the first ten-section production bundle. Section 7 carries what the reviewer directed; section 8 carries what a human cited; the document itself states the distinction:

> Remediation evidence is human-cited. OpenSoyce validates that evidence is present and referenced; it does not verify the fix.

> A recorded direction is not completed remediation; the evidence above is what a human cited as closing the loop.

Export artifact, verbatim as generated by production:

```txt
docs/proof/artifacts/pr-proof-3/lodash-evidence-loop-closed-export.md
```

## Honest edges

* The evidence is **human-cited**. OpenSoyce validated that the citation exists and is referenced; it did not verify that lodash@4.17.21 replaced 4.17.20 in any deployed project. Evidence *verification* is deliberately future work.
* The follow-up `lodash@4.17.21` ingest is itself a purpose-built dogfood observation — made through the real production CLI, under the PR-PROOF-2 rule: accelerated, not fabricated.
* The evidence answers the second of the two `remediation_required` resolutions (the append-only honest edge from PR-PROOF-2 carries forward — both directions remain on the record).
* The long-running natural proof `b777fb25` remains untouched and expires 2026-07-10 on its own clock.

## What this proof does not claim

* that the vulnerability is fixed anywhere outside the cited records
* compliance certification
* evidence verification
* auditor or customer acceptance

## Doctrine confirmed

The reviewer direction opened the case.

The human closed it with a citation.

The system validated presence, recorded, and remembered.

Nothing in the past was edited. Nothing was certified.

Not "we fixed the vuln" — "we recorded evidence that the human says closes the remediation loop."
